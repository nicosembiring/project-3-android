<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.harunnico.project3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_truk"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_truk_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GantiActivity"></activity>
        <activity
            android:name=".ConfirmActivity"
            android:label="Konfirmasi Pengiriman" />
        <activity android:name=".DirectionsActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Splash" />
        <activity
            android:name=".HomeActivity"
            android:label="Home"></activity>
        <activity
            android:name=".EditActivity"
            android:label="Edit Data Pengemudi" />
        <activity
            android:name=".UserActivity"
            android:label="Profil Pengemudi" />
        <activity android:name=".NavigationActivity" />
        <activity android:name=".HasilActivity" />
        <activity
            android:name=".DataActivity"
            android:label="Data Barang" />
        <activity
            android:name=".DetailActivity"
            android:label="Detail Barang" />
        <activity
            android:name=".MainActivity"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>